<div class="panel panel-default">
        <div class="panel-body">
            <a href="{{ path('add_subject', {'tbl': timetable.id} ) }}" class="btn btn-info">Add More Subjects</a>
            <a href="{{ path('list_teachers', {'tbl': timetable.id} ) }}" class="btn btn-info">Teachers List</a>
            <a href="{{path('homepage')}}" class="btn btn-danger">DONE</a>
            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                <thead>
                    <tr>
                        <th>Title</th>
                        {# <th>Code</th> #}
                        <th>Manage</th>
                    </tr>
                </thead>
                <tbody>
                {% for subject in subjects %}
                    <tr class="odd gradeX">
                        <td><a href="">{{ subject.sTitle }}</a></td>
                        {# <td>{{ subject.subjectCode }}</td> #}
                        <td class="center">
                            <a class="btn btn-primary" href="{{ path('edit_subject', { 'subjectId': subject.id, 'tbl': timetable.id } ) }}">Edit</a>
                            <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal{{ subject.id }}">Delete</button>
                        </td>
                    </tr>
                    <!-- Modal -->
                    <div class="modal fade" id="deleteModal{{ subject.id }}" role="dialog">
                        <div class="modal-dialog modal-sm">
                          <div class="modal-content">
                            <div class="modal-header">
                              <button type="button" class="close" data-dismiss="modal">&times;</button>
                              <h4 class="modal-title">Are you sure?</h4>
                            </div>
                            <div class="modal-body">
                              <p>Are you sure you want to delete {{ subject.sTitle }} ?.</p>
                            </div>
                            <div class="modal-footer">
                              <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                              <a href="{{ path('delete_subject', { 'subjectId': subject.id, 'tbl': timetable.id } ) }}" class="btn btn-danger">Go Ahead</a>
                            </div>
                          </div>
                        </div>
                    </div>

                {% endfor %}
                </tbody>
            </table>
            <!-- /.table-responsive -->
        </div>
        <!-- /.panel-body -->
    </div>
  